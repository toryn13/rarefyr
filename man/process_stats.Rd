% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/process_stats.R
\name{process_stats}
\alias{process_stats}
\title{Process VSEARCH .stats files and extract read retention for a chosen MaxEE level}
\usage{
process_stats(directory, maxEE_level, output_csv = "maxEE_summary.csv")
}
\arguments{
\item{directory}{Character. Path to directory containing \code{.stats} files.}

\item{maxEE_level}{Character. One of \code{"MaxEE0.5"}, \code{"MaxEE1"}, \code{"MaxEE2"}.}

\item{output_csv}{Character. Output CSV filename (written inside \code{directory}).
Default \code{"maxEE_summary.csv"}.}
}
\value{
A data.frame with columns \code{Length}, the selected MaxEE level, and \code{File}.
}
\description{
Reads all \code{.stats} files in a directory, parses read-length retention
tables produced by VSEARCH, and extracts counts for a chosen MaxEE level.
Handles VSEARCH's irregular spacing inside the parenthetical percentages
(e.g. \code{"376( 94.5\%)"}) via regex rather than whitespace splitting.
}
