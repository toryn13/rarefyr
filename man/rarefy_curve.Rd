% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rarefy_curve.R
\name{rarefy_curve}
\alias{rarefy_curve}
\title{Compute rarefaction curves for an OTU/ASV table}
\usage{
rarefy_curve(
  otu_table,
  max_depth = NULL,
  step = 50,
  num_iterations = 10,
  seed = NULL
)
}
\arguments{
\item{otu_table}{A matrix or data frame of counts with taxa/OTUs as
rows and samples as columns. Row names should be OTU/taxon IDs.}

\item{max_depth}{Maximum sequencing depth to rarefy to. Defaults to
the minimum total count across samples (i.e. the depth common to
all samples). Samples with fewer total counts than \code{max_depth}
are excluded (with a warning).}

\item{step}{Number of depth points to compute along the curve.
Default 50.}

\item{num_iterations}{Number of resampling iterations per depth.
Higher values give smoother curves at the cost of speed.
Default 10.}

\item{seed}{Optional integer seed for reproducibility.}
}
\value{
A tibble with columns \code{sample}, \code{depth}, \code{richness} (mean
observed richness across iterations) and \code{richness_sd} (standard
deviation across iterations).
}
\description{
Subsamples each sample in an OTU/ASV count table to a series of
sequencing depths and records the mean (and SD) observed richness
across repeated resamples. Useful for checking whether sequencing
depth was sufficient to capture community diversity.
}
\details{
At each depth, \code{num_iterations} subsamples are drawn using
\code{stats::rmultinom()}, which resamples reads \emph{with} replacement in
proportion to each OTU's relative abundance. This matches the
classic rarefaction resampling model (equivalent to drawing reads
one at a time and counting uniques, but computed directly via the
multinomial distribution for speed). It is a close approximation to
the exact (without-replacement / hypergeometric) rarefaction used by
e.g. \code{vegan::rarefy()}; for most teaching and QC purposes the two are
indistinguishable.
}
\examples{
set.seed(1)
otu <- matrix(rpois(20 * 6, lambda = 5), nrow = 20,
              dimnames = list(paste0("OTU", 1:20), paste0("S", 1:6)))
curve <- rarefy_curve(otu, num_iterations = 5, seed = 1)
head(curve)

}
