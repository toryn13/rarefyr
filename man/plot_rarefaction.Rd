% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_rarefaction.R
\name{plot_rarefaction}
\alias{plot_rarefaction}
\title{Plot rarefaction curves}
\usage{
plot_rarefaction(
  rarefaction_df,
  metadata = NULL,
  group_var = NULL,
  show_error_ribbon = TRUE
)
}
\arguments{
\item{rarefaction_df}{Output of \code{\link[=rarefy_curve]{rarefy_curve()}}, with columns
\code{sample}, \code{depth}, \code{richness}, and (optionally) \code{richness_sd}.}

\item{metadata}{Optional data frame with a \code{sample} column, used to
color curves by a grouping variable instead of by individual
sample.}

\item{group_var}{Name (string) of the column in \code{metadata} to color
by. Required if \code{metadata} is supplied.}

\item{show_error_ribbon}{Logical; if \code{TRUE} and \code{richness_sd} is
present, draw a +/- 1 SD ribbon around each curve. Default \code{TRUE}.}
}
\value{
A ggplot object.
}
\description{
Plot rarefaction curves
}
\examples{
set.seed(1)
otu <- matrix(rpois(20 * 6, lambda = 5), nrow = 20,
              dimnames = list(paste0("OTU", 1:20), paste0("S", 1:6)))
curve <- rarefy_curve(otu, num_iterations = 5, seed = 1)
plot_rarefaction(curve)

}
